# EdgeX Ops Intelligence same-origin reverse proxy sample for a host-level
# Nginx running on the Docker host.
#
# Assumptions:
# - deploy/docker-compose.yaml publishes web on 127.0.0.1:3001.
# - deploy/docker-compose.yaml publishes backend on 127.0.0.1:8080.
# - TLS/certificates are managed by the host or an upstream AWS load balancer.
# - /metrics should be restricted to trusted monitoring networks.

server {
    listen 80;
    server_name ops-intelligence.example.com;

    client_max_body_size 10m;

    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    location /api/ {
        # Backend is published on the host loopback address by Compose.
        proxy_pass http://127.0.0.1:8080;
    }

    location = /metrics {
        # Prometheus scrape endpoint. Replace this with the monitoring CIDR in
        # AWS DEV/prod; do not expose /metrics publicly.
        allow 127.0.0.1;
        deny all;
        proxy_pass http://127.0.0.1:8080;
    }

    location / {
        # Web is published on the host loopback address by Compose. This also
        # serves /healthz from the Next.js container.
        proxy_pass http://127.0.0.1:3001;
    }
}
